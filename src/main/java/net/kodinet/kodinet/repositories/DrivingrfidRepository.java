package net.kodinet.kodinet.repositories;

import net.kodinet.kodinet.entities.DrivingRfid;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DrivingrfidRepository extends JpaRepository<DrivingRfid,String> {
    
}
