package net.kodinet.kodinet.repositories;

import net.kodinet.kodinet.entities.FiscalAdministration;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FiscalAdministrationRepository extends JpaRepository<FiscalAdministration,Long> {
}
